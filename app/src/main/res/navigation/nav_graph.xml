<!-- res/navigation/nav_graph.xml -->

<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.seo.todayweather.ui.home.HomeFragment"
        android:label="HomeFragment"
        tools:layout="@layout/fragment_home">
        <!-- Additional fragment configuration, if needed -->
    </fragment>

    <fragment
        android:id="@+id/commendFragment"
        android:name="com.seo.todayweather.ui.commend.CommendFragment"
        android:label="CommendFragment"
        tools:layout="@layout/fragment_commend">
        <!-- Additional fragment configuration, if needed -->
    </fragment>

    <fragment
        android:id="@+id/styleFragment"
        android:name="com.seo.todayweather.ui.style.StyleFragment"
        android:label="StyleFragment"
        tools:layout="@layout/fragment_style">
        <!-- Additional fragment configuration, if needed -->
    </fragment>

    <fragment
        android:id="@+id/myPageFragment"
        android:name="com.seo.todayweather.ui.mypage.MyPageFragment"
        android:label="MyPageFragment"
        tools:layout="@layout/fragment_my_page">
        <!-- Additional fragment configuration, if needed -->
    </fragment>

    <!-- Additional navigation paths, if needed -->

</navigation>
